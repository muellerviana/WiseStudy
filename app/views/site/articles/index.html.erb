<div class="w-full h-full items-center grid grid-cols-4 gap-4">
  <% @articles.each do |article| %>
    <%= render(CardComponent.new(title: article.title, body: article.description)) %>
  <% end %>
</div>